import React from 'react'
import ReactDOM from 'react-dom/client'
import MEMSDashboard from './MEMSDashboard'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <MEMSDashboard />
    </React.StrictMode>,
)
